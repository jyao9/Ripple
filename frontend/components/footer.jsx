var React = require('react');

var Footer = React.createClass({
  render: function () {
    return(
      <footer className="footer">
        <ul>
          <li>Ripple</li>
        </ul>
      </footer>
    );
  }
});

module.exports = Footer;
